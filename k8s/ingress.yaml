apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: axion-ingestion-service-ingress
  annotations:
    konghq.com/plugins: rate-limiting-plugin, cors-plugin
spec:
  ingressClassName: kong
  rules:
    - host: ingestion.axion.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: axion-ingestion-service
                port:
                  number: 8000
