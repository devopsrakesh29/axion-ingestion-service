apiVersion: v1
kind: Service
metadata:
  name: {{ include "axion-ingestion-service.fullname" . }}
  labels:
    {{- include "axion-ingestion-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "axion-ingestion-service.selectorLabels" . | nindent 4 }}
